# Trust Engine get-validator-data

> Get a validator's data from the Trust Engine system

The get-validator-data command allows you to retrieve detailed information about a Trust Engine validator, including their reputation score, voting history, and performance metrics within the Trust Engine validation network.

To use it, run the following command from your terminal

```bash
trust-engine-cli get-validator-data [options]
```

## Options

| Option                      | Description                                       |
| --------------------------- | ------------------------------------------------- |
| `-w, --walletAddress <address>` | Wallet address of the validator (Solana public key) |
| `--json`                    | Output the result as JSON                         |

## Example

Get validator data for a specific wallet address:

```bash
trust-engine-cli get-validator-data --walletAddress "9WzDXwBbmkg8ZTbNMqUxvQRAyrZzDsGYdLVL9zYtAWWM"
```

After running the command, you'll see the validator's Trust Engine data:

```
✅ Validator data retrieved successfully.

Validator Data for 9WzDXwBbmkg8ZTbNMqUxvQRAyrZzDsGYdLVL9zYtAWWM:
  - Validator Address: 9WzDXwBbmkg8ZTbNMqUxvQRAyrZzDsGYdLVL9zYtAWWM
  - Validator PDA: xyz789abc123def456ghi789
  - Staked Amount: 1000000
  - Reputation Score: 85.5
  - Total Votes: 1250
  - Honest Votes: 1200
  - Dishonest Votes: 50
  - Accuracy Percentage: 96%
  - Last Active Time: 1705308645
  - Last Active Date: 2024-01-15T10:30:45Z
```

For automation purposes, use the JSON output:

```bash
trust-engine-cli get-validator-data --walletAddress "9WzDXwBbmkg8ZTbNMqUxvQRAyrZzDsGYdLVL9zYtAWWM" --json
```

This will return:

```json
{
  "message": "Validator data retrieved successfully.",
  "details": {
    "validatorAddress": "9WzDXwBbmkg8ZTbNMqUxvQRAyrZzDsGYdLVL9zYtAWWM",
    "validatorPDA": "xyz789abc123def456ghi789",
    "stakedAmount": "1000000",
    "reputationScore": "85.5",
    "totalVotes": "1250",
    "honestVotes": "1200",
    "dishonestVotes": "50",
    "accuracyPercentage": "96",
    "lastActiveTime": "1705308645",
    "lastActiveDate": "2024-01-15T10:30:45Z"
  }
}
```
